/**
 * Figma node 196:542 — frame “float-atoms” (SVG export).
 * Asset: /public/images/float-atoms.svg
 */
export function FloatAtoms({ className }) {
    return (
        <img
            src="/images/float-atoms.svg"
            alt=""
            width={842}
            height={683}
            className={['h-auto max-w-full', className].filter(Boolean).join(' ')}
            role="presentation"
            decoding="async"
        />
    );
}
