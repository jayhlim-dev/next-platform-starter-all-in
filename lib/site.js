/** Canonical homepage URL (trailing slash; must match metadataBase). */
export const siteUrl = 'https://scalebiopartners.com/';

/** Short site label for Google site name and og:site_name (distinct from the default document title). */
export const siteName = 'ScaleBio Partners';

/** Default meta description (layout, manifest, Open Graph). */
export const siteDescription =
    'Providing personalized support to transform biotech innovations from breakthrough to business';
