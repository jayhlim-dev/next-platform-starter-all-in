[build]
  publish = ".next"
  command = "npm run build"

# Hostname is public (see lib/smtp-host.js). Omit if the value is still set as a Netlify “secret”.
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SMTP_HOST"